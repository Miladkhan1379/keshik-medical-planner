{
  "name": "keshikyar-mobile",
  "version": "1.1.1",
  "private": true,
  "description": "KeshikYar mobile wrapper for Android and iOS",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "mobile:add:android": "npm run mobile:prepare && npx cap add android",
    "mobile:add:ios": "npm run mobile:prepare && npx cap add ios",
    "mobile:sync": "npm run mobile:prepare && npx cap sync",
    "mobile:open:android": "npm run mobile:prepare && npx cap sync android && npx cap open android",
    "mobile:open:ios": "npm run mobile:prepare && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/filesystem": "latest-8",
    "@capacitor/share": "latest-8"
  },
  "devDependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  }
}
